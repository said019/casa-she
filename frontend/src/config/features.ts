/**
 * Feature flags de Casa Shé.
 *
 * RECEPCIÓN: apagada por ahora. El código (rutas /reception/*, layout, pantallas,
 * reportes por recepcionista, estado de cajas) se deja intacto para reactivarla
 * un día: solo cambia RECEPTION_ENABLED a `true` y todas sus superficies vuelven
 * a aparecer en la UI (nav admin, dashboard de cajas, reportes, etc.).
 */
export const RECEPTION_ENABLED = false;
