# BMB Studio — Frontend (Vite)
# Copia este archivo a .env y ajusta los valores.

# URL base de la API (backend). En local apunta al backend en el puerto 3001.
# En producción, la URL pública del backend (ej. https://api.bmbstudio.mx/api).
VITE_API_URL=http://localhost:3001/api
