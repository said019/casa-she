# Cobertura funcional — Casa Shé (auditoría pilates-studio-auditor)

> Matriz de ~110 escenarios (dominios A-L) + 25 edge cases, recorrida contra el código real. Evidencia citada archivo:línea; sin evidencia = ❓ o ❌, nunca ✅ por suposición.

## A — Registro e identidad

| ID | Estado | Sev. | Evidencia / Gap |
|---|---|---|---|
| A1 | ⚠️ | 🟠 | Hoy: el registro valida el FORMATO del teléfono (+52 y 10 dígitos) pero no verifica que el número le pertenezca a quien se registra — no hay envío/validación de OTP por WhatsApp/SMS, ni confirmación de email; la cuenta queda activa y logueada en el mismo request. Debería: agregar un paso de verificación (código de 6 dígitos vía WhatsApp Evolution API, ya integrada) antes de dar por completado el registro, en routes/auth.ts POST /register (o un nuevo POST /register/verify). |
| A2 | ✅ | 🟠 | backend/src/routes/auth.ts:344-408 (POST /forgot-password: genera JWT de 1h, envía email con link y, si hay teléfono, el mismo link por WhatsApp) y :413-448 (POST /reset-password: valida token y actualiza password); respuesta siempre genérica para evitar enumeración de emails. |
| A3 | ✅ | 🟠 | backend/src/routes/users.ts:900-984 (PUT /api/users/:id: la propia clienta edita displayName/email/phone/dateOfBirth/emergencyContact/healthNotes; valida email duplicado con 409); el teléfono se LEE en vivo de la tabla users en cada notificación futura, no se cachea en otro lado — backend/src/routes/bookings.ts:836-860 y :1520-1537 hacen `SELECT phone FROM users WHERE id=$1` justo antes de cada envío de WhatsApp. |
| A4 | ✅ | 🟠 | frontend/src/components/onboarding/ProfilerGate.tsx (gate obligatorio para clientas nuevas, montado desde AuthGuard cuando onboarding_required && !onboarding_completed_at) + backend/src/routes/onboarding.ts:12-100 (POST /onboarding/submit valida preguntas de salud embarazo/lesión/condición y escribe health_notes); visible a la instructora SOLO de sus propias clases en backend/src/routes/classes.ts:1222-1261 (GET /api/classes/:id/attendees, con chequeo de ownership antes de exponer health_notes). |
| A5 | ⚠️ | 🟡 | Hoy: se guarda un único timestamp de aceptación, sin versión del reglamento. Si el estudio cambia el texto del reglamento, las clientas que ya aceptaron uno viejo nunca son forzadas a re-aceptar. Debería: agregar reglamento_version en system_settings y reglamento_accepted_version en users; el gate (ReglamentoGate.tsx + el 403 de bookings.ts) debe comparar versión vigente vs. versión aceptada, no solo NULL vs NOT NULL. |
| A6 | ❌ | 🟡 | Hoy: cualquier persona puede registrarse sin importar su edad; el aviso de privacidad es solo texto sin enforcement. Debería: calcular edad a partir de date_of_birth en POST /register (o en el ProfilerWizard) y, si es menor a la política del estudio, bloquear el auto-registro o exigir capturar datos de tutor (nombre, teléfono, parentesco) antes de activar la cuenta — viviría en types/auth.ts RegisterSchema + una tabla guardian_info. |
| A7 | ✅ | 🟠 | backend/src/routes/users.ts:581-695 (POST /api/users, rol admin/super_admin/reception: alta con nombre+email+teléfono, password temporal generada, credenciales enviadas por email y WhatsApp para que la clienta pueda entrar directo) y backend/src/routes/migrations.ts:48-127 (POST /api/migrations/client: busca por email o teléfono antes de crear, para no duplicar si la clienta ya existe). |
| A8 | ⚠️ | 🟡 | Hoy: el gate de compra solo revisa si HAY un paquete activo en este momento, no si el usuario YA compró una Clase Muestra alguna vez (routes/orders.ts:531-541 vía canBuySamplePlan solo cuenta memberships activas) — una clienta puede volver a comprar la Clase Muestra repetidamente mientras deje vencer sus paquetes entre medio, rompiendo el '1 por persona'. Además no hay reporte de conversión trial→pagado (sin resultados en routes/reports.ts ni routes/stats.ts para 'sample'), solo el flag crudo sample_class_discount_used en BD. Debería: sumar un chequeo histórico (orders.package_type='sample' AND status='approved' alguna vez para ese user_id) en canBuySamplePlan, y agregar una métrica de conversión en el panel de reportes. |
| A9 | ⚠️ | 🟡 | Hoy: (1) no hay autoservicio — la clienta no puede solicitar/ejecutar el borrado de su cuenta desde su perfil, solo un admin vía panel, y el canal 'oficial' es escribir un correo fuera del sistema; (2) el borrado es literal (los registros propios del cliente en bookings/memberships/payments/orders son ON DELETE CASCADE, según el comentario en users.ts:1216-1221), es decir se PIERDE el historial contable en vez de conservarse anonimizado; (3) no define explícitamente qué pasa con un paquete activo (reembolso vs. pérdida) al momento del borrado, simplemente cae en la cascada. Debería: agregar un flujo de anonimización (reemplazar PII por placeholders, conservar montos/fechas de payments/orders) distinto del hard delete, accesible por la clienta autenticada sobre su propia cuenta, con un paso explícito de qué pasa con el saldo activo. |
| A10 | ❌ | 🟡 | Hoy: si la misma persona queda registrada dos veces (con/sin acento, dos teléfonos), no hay ninguna detección automática de posibles duplicados ni herramienta para fusionarlas conservando pagos/clases/historial de ambas — recepción tendría que arreglarlo a mano en la BD. Debería: construir un endpoint análogo al de instructors.ts (p.ej. POST /api/users/:id/merge) que reasigne bookings/memberships/payments/orders/loyalty_points del duplicado al registro principal y desactive el sobrante, más una vista en el panel admin (routes/search.ts o routes/clients.ts) que sugiera posibles duplicados por nombre similar/mismo teléfono normalizado. |

## B — Horarios y reservas

| ID | Estado | Sev. | Evidencia / Gap |
|---|---|---|---|
| B1 | ✅ | 🔴 | frontend/src/components/Schedule.tsx:90-128 (GET /classes por semana; spots=capacity-current_bookings en tiempo real, filtros sucursal/categoría/instructor/hora, refresco vía React Query staleTime 2min); backend/src/routes/classes.ts:55-137 GET / (current_bookings/max_capacity en vivo, Cache-Control:no-store para usuarios autenticados). Generación fiel a la plantilla: routes/classes.ts:545-665 POST /generate copia class_type_id/instructor_id/facility_id/start_time/end_time/max_capacity directo de `schedules` (líneas 627-641), salta días cerrados (studio_closed_days) y evita duplicados (schedule_id+date, ON CONFLICT DO NOTHING); cron diario services/cron-jobs.ts:74-156 generateRecurringClasses + cron.schedule('0 3 * * *', ..., {timezone:'America/Mexico_City'}) línea 863. Nota menor (no bloqueante): generateRecurringClasses (cron-jobs.ts:107-111) mezcla Date.getDate()/setDate() (métodos locales) con getUTCDay()/toISOString() (UTC) — el mismo patrón que classes.ts sí evita explícitamente en /generate y /recurring ('todo en UTC para evitar el bug de +1 día', líneas 423 y 596). Con el contenedor en TZ=UTC (default Railway/nixpacks) y el cron disparando a las 3am CDMX (lejos de la medianoche), esto no se manifiesta hoy como bug, pero es una inconsistencia de código frente al patrón que el propio equipo documentó como riesgo. |
| B2 | ✅ | 🔴 | backend/src/routes/bookings.ts:404-902 POST / — transacción con FOR UPDATE sobre la clase (línea 644-647) y la membresía (dentro de selectMembershipForBooking), orden de locks clase→membresía documentado explícitamente; selección determinista de membresía por categoría (reformer_remaining vs multi_remaining) vía lib/membershipSelection.ts:113-157 (acotada antes que ilimitada, vigencia más próxima, más antigua); descuento auto-guardado (`WHERE ... > 0`, línea 779) que detecta carreras; confirmación instantánea (status='confirmed' en el INSERT, línea 795-800); notificación WhatsApp/email/push/wallet no bloqueante tras el commit (líneas 822-894). |
| B3 | ✅ | 🟠 | backend/src/routes/reformers.ts:9-78 GET /by-class/:classId (mapa con is_occupied/is_mine/occupied_by_name, respeta layout real vía position_x/y/rotation/scale); líneas 98-154 POST /assign con lock optimista (índice único → 409 'Alguien más acaba de tomar ese lugar' si hay carrera) y validación de que el lugar pertenece a la sala de la clase; frontend/src/pages/client/SelectReformer.tsx (selector visual, refetchInterval 10s para ver ocupación en vivo). |
| B4 | ✅ | 🟠 | backend/src/lib/waitlist.ts:65-144 joinWaitlist (posición = MAX(waitlist_position)+1, valida política/corte de horas/tamaño máximo de fila/crédito de la categoría SIN cobrarlo, todo bajo FOR UPDATE de la clase); routes/bookings.ts:444-454 ofrece unirse a la fila cuando la clase está llena; frontend/src/pages/client/BookClassConfirm.tsx:270 mensaje explícito 'Clase llena — puedes anotarte en lista de espera' y línea 307 CTA 'Anotarme en lista de espera' con toast de posición (línea 122). |
| B5 | ⚠️ | 🟠 | La promoción CONFIRMA y COBRA el crédito de inmediato (waitlist.ts:220-224, `UPDATE bookings SET status='confirmed'` en el mismo paso que descuenta el crédito) en vez de darle a la clienta una ventana explícita para aceptar o rechazar el lugar ('tienes 30 min para confirmar'). No existe estado intermedio ni columna de expiración (`expires_at`/`pending_confirmation`) en el schema — si la clienta ya no quiere el lugar, el crédito ya se gastó y tiene que cancelar ella misma después. El fix viviría en lib/waitlist.ts (nuevo estado 'offered' + expiración) más un cron de barrido en services/cron-jobs.ts, similar a los demás jobs por tiempo. |
| B6 | ✅ | 🟡 | backend/src/lib/settings.ts:33-41,104-112 BookingPolicies (max_advance_days, min_hours_before_booking) con defaults editables; aplicado en routes/bookings.ts:524-559 (bloquea con 400 si excede la anticipación máxima o no cumple la anticipación mínima, comparando en zona CDMX vía `toLocaleString('en-US',{timeZone:'America/Mexico_City'})` línea 545); editable sin tocar código desde frontend/src/pages/admin/settings/PoliciesSettings.tsx:117-176. |
| B7 | ⚠️ | 🟡 | No existe límite de 'reservas simultáneas máximas' (activas a la vez, sin importar el día) ni bloqueo de 'no reservar dos clases que se empalman' (mismo horario o traslapado). El único constraint de unicidad en bookings es UNIQUE(class_id, user_id) (backend/database/schema_complete.sql:484) — nada impide que una clienta reserve, por ejemplo, Pilates 9:00-9:50 y Barre 9:15-10:00 el mismo día. routes/schedules.ts:89 trae incluso un TODO explícito ('Add overlap check logic here if needed') pero referido a horarios de instructoras/salas, no a reservas de clientas. El fix viviría en el bloque de políticas de routes/bookings.ts (~línea 511, junto a max_bookings_per_day) más los campos correspondientes en booking_policies/PoliciesSettings.tsx. |
| B8 | ✅ | 🔴 | backend/src/routes/bookings.ts usa el código NEEDS_PURCHASE de forma consistente: línea 719 (membresía vencida para la fecha de la clase), línea 727-730 (sin créditos de la categoría), línea 758-761 (sin membresía válida en auto-selección), línea 785 (sin créditos tras perder una carrera de descuento); frontend/src/pages/client/BookClassConfirm.tsx:97-103 detecta ese código y redirige automáticamente a /app/checkout con mensaje 'Te llevamos a comprar un paquete o membresía' — no es un error genérico. |
| B9 | ⚠️ | 🟡 | No hay flujo self-service desde la app de la CLIENTA para 'traer a una amiga': ella no puede registrar y reservar a una invitada por su cuenta (descontando 2 créditos propios o pagando un pase de invitada) — depende de llamar o ir a recepción para que el staff lo haga vía GuestBookingDialog. Faltaría un endpoint tipo POST /bookings/guest bajo auth de cliente (reutilizando /users/guest + selectMembershipForBooking con requiredCredits:2) más UI en BookClassConfirm.tsx / MyBookings.tsx. |
| B10 | ⚠️ | ⚪ | Ese motor es exclusivo de ADMIN (`requireRole('admin')`, línea 170) — la clienta no tiene, dentro de su propia app, un botón 'reservar todas las semanas' o suscripción recurrente (no aparece en pages/client/BookClasses.tsx, BookClassConfirm.tsx ni MyBookings.tsx). Solo recepción/admin puede hacerlo por ella. Exponer el mismo motor (acotado a sus propios horarios habituales) como self-service en el cliente cerraría el hueco; severidad baja porque recepción puede cubrirlo manualmente hoy. |
| B11 | ⚠️ | 🟡 | No existe un sistema de 'strikes' ni bloqueo temporal de reservas anticipadas para clientas con no-shows repetidos — la única consecuencia hoy es perder el crédito de ESA clase, sin escalamiento. El flag `no_show_penalty` (backend/src/lib/settings.ts:35 y default línea 106; también sembrado en backend/src/index.ts:1081) está definido en el tipo y en el default, pero se confirmó por grep que NUNCA se lee ni se aplica en ningún endpoint o cron — es una bandera muerta. El fix viviría en el bloque de validaciones de routes/bookings.ts POST / (contar no_shows recientes del usuario en una ventana configurable, mismo patrón que max_bookings_per_day líneas 561-583) más su UI en PoliciesSettings.tsx. |
| B12 | ✅ | 🟠 | frontend/src/pages/client/MyBookings.tsx — secciones 'Próximas' y 'Pasadas' (líneas 45-46, 61-62) ordenadas por fecha/hora, con badge de posición de lista de espera (líneas 107-111); backend/src/routes/bookings.ts:1038-1060 GET /my-bookings sobre `user_bookings_view`. |
| B13 | ✅ | 🟠 | backend/src/routes/bookings.ts:907-1033 POST /admin-book (requireRole 'admin','super_admin','reception') reutiliza selectMembershipForBooking con las mismas validaciones de saldo/vigencia/estudio que la ruta de la clienta, con FOR UPDATE sobre clase y membresía (mismo orden de locks que POST / para evitar deadlocks), y bypass de sobrecupo (`force`) restringido solo a admin/super_admin — recepción no puede forzar; frontend/src/pages/reception/BookingsScreen.tsx:599-661 con buscador de clienta por nombre/teléfono y botón de reserva. |

## C — Cancelaciones, cambios y waitlist

| ID | Estado | Sev. | Evidencia / Gap |
|---|---|---|---|
| C1 | ✅ | 🔴 | backend/src/index.ts:1939-1949 (función SQL cancel_booking, Migration 025) devuelve el crédito a la columna correcta según booking.consumed_category (reformer_remaining vs multi_remaining). El trigger trigger_update_booking_count (backend/database/schema_complete.sql:1480-1508) decrementa classes.current_bookings al cancelar, liberando el cupo. backend/src/routes/bookings.ts:1498-1510 llama a promoteNextFromWaitlist(...) inmediatamente después de la cancelación cuando se liberó un cupo real (dispara B5), y backend/src/lib/waitlist.ts:158-265 hace la promoción con re-resolución de membresía y notificación in-app/push. |
| C2 | ✅ | 🟠 | backend/src/index.ts:1904-1911 y 1996-2103 (cancel_booking / preview_cancel_booking): fuera de la ventana (min_hours configurable) o con la clase ya iniciada, la cancelación se bloquea con código explícito (CANCELLATION_WINDOW_EXCEEDED, CLASS_ALREADY_STARTED). backend/src/routes/bookings.ts:1321-1356 expone GET /:id/cancel-preview que el frontend consulta ANTES de mostrar el botón: frontend/src/pages/client/ClassBookingDetail.tsx:170-222 oculta 'Cancelar reserva' y muestra el mensaje de política (late_cancel_message) cuando canCancel=false, o el hint 'Tu crédito vuelve' / 'NO devuelve crédito' cuando sí puede cancelar. Toda la política (enabled, min_hours, refund_credit_on_cancel, cancellations_per_membership, mensaje) es editable por admin en backend/src/routes/settings.ts:52-120 y frontend/src/pages/admin/settings/CancellationPolicy.tsx. |
| C3 | ⚠️ | 🟠 | El toggle 'según política' es cosmético: booking_policies.no_show_penalty (lib/settings.ts:35,106; index.ts:1081) se define pero NUNCA se lee en ningún archivo del backend (confirmado por grep de todo el repo) — no tiene ningún efecto real ni existe campo en el admin (frontend/src/pages/admin/settings/PoliciesSettings.tsx no lo expone), así que el no-show SIEMPRE penaliza sin que la dueña pueda cambiarlo de verdad. Además no existe ningún sistema de 'strike'/bloqueo automático que consuma ese conteo (B11): grep de 'strike' en backend/src y frontend/src no arroja resultados — el no-show queda registrado pero no dispara ninguna acción automática sobre reservas futuras de esa clienta. Fix: leer no_show_penalty en markNoShows (o mover a cancellation_policy) y agregar tabla/columna de conteo + bloqueo temporal configurable (viviría en routes/settings.ts + una tabla client_strikes o columna en users). |
| C4 | ❌ | 🟡 | Hoy: la clienta que quiere cambiarse de horario debe cancelar su reserva (pantalla 1) y luego buscar y reservar la otra clase por separado (pantalla 2), sin ningún flujo guiado ni garantía de que complete el segundo paso. Debería: un botón 'Cambiar de clase' en ClassBookingDetail.tsx que abra el selector de clases y ejecute cancelar+reservar como una operación atómica en el backend (nuevo endpoint tipo POST /bookings/:id/reschedule que reutilice cancel_booking() + la lógica de selectMembershipForBooking en una sola transacción), evitando que la clienta quede sin ninguna reserva si abandona el flujo a medias. |
| C5 | ⚠️ | 🔴 | Hoy: cancelClassWithRefunds solo envía un push web (lib/cancel-class.ts:57-60, sendWebPushToUser) a cada afectada — nunca WhatsApp. El envío de WhatsApp de cancelación (sendCancellationNotice, lib/whatsapp.ts:186) solo se invoca desde la cancelación individual iniciada por la clienta (routes/bookings.ts:1536), y grep confirma que NUNCA se llama desde cancelClassWithRefunds, classes.ts DELETE, closed-days.ts ni events.ts. Dado que WhatsApp es el canal primario en México y push es 'complemento' (perfil del estudio), una clienta sin la app abierta/con push desactivado nunca se entera de que el estudio canceló su clase. Tampoco existe lógica de 'sugerir horarios alternativos'. Debería: agregar el envío de sendCancellationNotice (o una plantilla específica de 'clase cancelada por el estudio') dentro de cancelClassWithRefunds en lib/cancel-class.ts, y opcionalmente anexar 2-3 clases del mismo tipo con cupo disponible en los próximos días. |
| C6 | ❌ | 🟡 | Hoy: la dueña cambia el coach de una clase con reservas y ninguna clienta se entera hasta llegar al estudio. Debería: cuando change-instructor o substitute afecte una clase con reservas activas, disparar un push/in-app (y opcionalmente WhatsApp, respetando el límite de 3 mensajes ya decidido) tipo 'class_updated' con el nombre del nuevo coach — viviría en routes/classes.ts, reutilizando bookedClients (ya se consulta en substitute, línea 1139) y el tipo class_updated ya definido pero sin cablear en lib/in-app-notifications.ts. |
| C7 | ❌ | 🟠 | Hoy: la dueña puede mover fecha/hora de una clase con reservas activas sin que el sistema le advierta, sin ofrecerle elegir 'solo esta clase o toda la serie', y sin notificar ni dar opción de cancelar sin penalización a las clientas ya reservadas. Debería: en PUT /:id, si data.date/startTime/endTime cambian y existing.current_bookings > 0, exigir confirmación explícita (patrón similar a PATCH /:id/free con code HAS_FREE_BOOKINGS) y, al confirmar, notificar a cada reservada (push/in-app/WhatsApp) con opción de cancelar sin penalización (reutilizando cancel_booking con forceRefund=true). Viviría en routes/classes.ts PUT /:id + frontend ClassesCalendar.tsx (diálogo de edición). |
| C8 | ✅ | ⚪ | backend/src/index.ts:1871-1880 (rama waitlist dentro de cancel_booking()): salir de la lista de espera se procesa sin ventana de tiempo, sin reembolso (no se había cobrado crédito) y sin quemar el contador de cancelaciones. frontend/src/pages/client/ClassBookingDetail.tsx:170-186 muestra el botón 'Salir de la lista de espera' con el texto aclaratorio 'Salir de la lista no gasta créditos ni cuenta como cancelación', usando el mismo endpoint POST /bookings/:id/cancel. |

## D — Paquetes y membresías

| ID | Estado | Sev. | Evidencia / Gap |
|---|---|---|---|
| D1 | ✅ | 🟠 | backend/src/routes/plans.ts:36-107 (GET /api/plans y GET /api/plans/:id) expone name, description, price, currency, duration_days, class_limit, reformer_credits/multi_credits, features (bullets libres), is_internal (oculta planes internos a clientas) y requires_studio_selection (restricción por sucursal). Renderizado en frontend/src/pages/admin/plans/PlansList.tsx. |
| D2 | ✅ | 🔴 | backend/src/lib/orderFulfillment.ts:11-118 finalizePaidOrder() crea la membresía, marca la orden aprobada, otorga puntos y notifica — llamado desde routes/mercadopago-webhook.ts:82, routes/stripe-webhook.ts:41 y routes/orders.ts:784 (sync manual). Idempotente vía SELECT...FOR UPDATE dentro de la transacción (líneas 27-41), así que webhook duplicado o doble click no activa dos veces. |
| D3 | ✅ | 🔴 | backend/src/routes/memberships.ts:59-132 (GET /me) devuelve classes_remaining, reformer_remaining, multi_remaining, end_date y totales agregados de todas las membresías activas (total_reformer_available, total_multi_available, has_multiple_memberships). frontend/src/pages/client/ProfileMembership.tsx:80-133 lo muestra con progreso visual + botón renovar. También se refleja en WhatsApp (recordatorio D4/H5) y en wallet pass vía notifyMembershipRenewed/notifyMembershipExpiring (backend/src/lib/notifications.ts:132-162). |
| D4 | ⚠️ | 🟠 | Hoy: el trigger está hardcodeado a `daysToNotify = [1]` (línea 248) — solo avisa 1 día antes, sin el escalón intermedio (p.ej. 5 días antes) ni ningún recordatorio basado en 'te quedan 2 clases' para paquetes con crédito limitado que aún no llegan a su fecha de vencimiento. Tampoco es configurable desde el panel (no hay una key en system_settings que el cron consulte). Debería vivir como config en routes/settings.ts + lib/settings.ts (nueva key p.ej. `membership_reminder_policy` con umbrales de días y de créditos) y notifyExpiringMemberships debería leerla en vez de tener el arreglo fijo. Además, todo el job depende del flag global ENABLE_CRON_JOBS (backend/src/index.ts:3750), que según memoria de sesiones previas ha estado apagado en prod — el estado real en producción no es verificable solo con el código. |
| D5 | ❌ | 🟠 | Hoy: en cuanto pasa la fecha de vencimiento la membresía queda 'expired' y selectMembershipForBooking (lib/membershipSelection.ts) deja de considerarla — los créditos sin usar simplemente se pierden, sin días de gracia y sin opción de 'congelables'. No existe ninguna key en system_settings que parametrice esta política (grep sobre routes/settings.ts solo muestra `cancellation_policy`, `booking_enabled`, `bank_info`, `payroll_config`). Debería: (a) agregar una política configurable (p.ej. `expiry_policy` con gracia en días o 'convertir a congelado'), consultada por markExpiredMemberships antes de expirar, y (b) UI en el panel de configuración para editarla — la matriz marca esto como 'la fuente #1 de conflictos con clientas', y hoy es comportamiento fijo en código, no una decisión operativa del dueño. |
| D6 | ✅ | 🟠 | Renovar = comprar de nuevo (checkout online D2, o /assign de staff D8): botón 'Renovar membresía' en frontend/src/pages/client/ProfileMembership.tsx:129-133 lleva a /app/checkout. El stacking (qué pasa si renueva antes de agotar la anterior) queda resuelto de forma determinista por D9 (lib/membershipSelection.ts pickBestMembership: la más próxima a vencer se consume primero) y el saldo agregado de ambas se ve en GET /me (has_multiple_memberships, total_*_available). |
| D7 | ⚠️ | 🟡 | Hoy: (1) no hay NINGUNA pantalla en frontend/src/pages/admin ni frontend/src/pages/client que llame a estos endpoints — confirmado por grep sin resultados sobre 'pause\|resume\|pauseMembership\|resumeMembership' en frontend/src; el endpoint existe pero es inalcanzable desde el producto real, solo vía llamada directa a la API. (2) Es exclusivamente de staff (requireRole admin/super_admin/reception) — la clienta no puede autopausar. (3) A diferencia de /assign, /:id/dates y /:id/credits, estos dos handlers NO llaman a logAction (backend/src/routes/memberships.ts:1076-1131) — no queda registro de auditoría de quién pausó/reanudó ni por qué (el 'reason' del schema es opcional, sin mínimo de caracteres). (4) No hay límites configurables (máx. X días, máx. Y veces al año) en ningún lugar del código. (5) No se define qué pasa con reservas futuras ya hechas contra esa membresía mientras está pausada (no se cancelan ni se marcan). Para cerrarlo: exponer un botón en ClientDetail.tsx (admin) y en ProfileMembership.tsx (cliente), agregar logAction + motivo obligatorio, y agregar límites configurables en system_settings. |
| D8 | ✅ | 🔴 | backend/src/routes/memberships.ts POST /assign (340-555), POST /assign-cash (562-713) y POST /:id/activate (718-965) aceptan `startDate` explícito (retroactivo) y `endDate` override vía computeEndDate() (líneas 28-39), registran el pago (method/monto/referencia) en la tabla payments ligado a la membresía, guardan activated_by/activated_at y generan logAction de auditoría. UI real: frontend/src/pages/admin/payments/CashAssignment.tsx, frontend/src/pages/admin/memberships/MembershipsList.tsx y frontend/src/pages/reception/ClientsScreen.tsx. |
| D9 | ✅ | 🟠 | backend/src/lib/membershipSelection.ts:70-97 pickBestMembership() implementa la regla explícita: elegible por estudio+categoría → acotada antes que ilimitada → vigencia más próxima (end_date asc, nulls al final) → created_at más antiguo como desempate. Usada consistentemente en backend/src/routes/bookings.ts (líneas 14, 287-321 reserva en bloque, 747 y 987 reserva individual/admin) y backend/src/lib/waitlist.ts (líneas 7, 117, 202) al promover desde lista de espera. selectMembershipForBooking (líneas 113-157) además bloquea con FOR UPDATE dentro de la transacción de reserva. |
| D10 | ❌ | ⚪ | No existe transferencia de créditos entre cuentas ni gift card con código canjeable. Es explícitamente opcional según la matriz (⚪, 'según el estudio'); si el dueño lo pide, viviría como un nuevo endpoint en routes/memberships.ts (transferir créditos entre user_id) o una tabla gift_cards + canje en checkout. |
| D11 | ✅ | 🟡 | backend/src/routes/discount-codes.ts:1-75+ — CRUD completo de códigos (percentage/fixed), max_uses/current_uses, valid_from/valid_until, min_purchase, restricción por plan (discount_code_plans) y por usuario específico (user_id), más is_referral. Se aplica en checkout vía order.discount_code_id y se consume en backend/src/lib/orderFulfillment.ts (consumeFounderFirstPackageDiscount se referencia también en routes/memberships.ts:441-447 para descuento de fundadora). |
| D12 | ✅ | 🟠 | backend/src/routes/memberships.ts: rama 'gratis' en POST /assign (379-389, 440-453) y POST /:id/activate (740-750, 823-837) exige isElevated() + motivo mínimo 5 caracteres, registra pago en $0 y logAction con {gratis:true, reason}. Ajuste manual de créditos (PATCH /:id/credits, líneas 1283-1311) y de vigencia (PATCH /:id/dates, líneas 1138-1248) exigen motivo mínimo 5 caracteres para recepción sin permiso 'creditos_sin_limite', limitan el delta a ±3 créditos por movimiento, y ambos generan logAction con oldData/newData completos. |
| D13 | ❌ | ⚪ | Hoy: si una clienta quiere subir de paquete a mitad de vigencia, no hay flujo dedicado — el único workaround es que recepción ajuste manualmente los créditos vía PATCH /:id/credits (backend/src/routes/memberships.ts:1253-1373), lo cual no calcula ninguna diferencia de precio ni dispara un pago adicional. Para cerrarlo habría que definir la política (cobra diferencia prorrateada vs. créditos se suman) y construir un endpoint tipo POST /memberships/:id/upgrade que la aplique con su propio logAction, en vez de depender del ajuste genérico de créditos. |

## E — Pagos

| ID | Estado | Sev. | Evidencia / Gap |
|---|---|---|---|
| E1 | ⚠️ | 🔴 | El camino de ÉXITO funciona bien (poll + activación vía webhook). Pero el de FALLO no: routes/mercadopago-webhook.ts:83-88 solo setea `rejected_at` y NUNCA cambia `orders.status` a 'rejected' para pagos con tarjeta rechazados/cancelados — la orden queda eternamente 'pending_payment'. OrderDetail.tsx no lee el query param `?mp=failure` ni muestra `mp_status_detail`, así que el cliente solo ve 'Esperando pago' sin mensaje de rechazo. Soporte de OXXO/SPEI: buildPreferenceBody (mercadopago.ts:30-55) no los excluye, pero si están habilitados es config de la cuenta MP, no verificable desde el código. |
| E2 | ✅ | 🔴 | backend/src/routes/payments.ts:136-199 (POST /payments/register: monto, moneda, método cash/transfer, reference, notes, processed_by, shift_id/facility_id vía openShiftForUser); backend/src/routes/memberships.ts:367-555 (POST /assign, permite rol reception) y :573+ (POST /assign-cash) crean la membresía y el payment ligado en una transacción, con processed_by=req.user.userId y shift/caja. |
| E3 | ✅ | 🔴 | backend/src/routes/mercadopago-webhook.ts:41-51 (INSERT ... ON CONFLICT (provider,event_key) DO NOTHING sobre payment_webhook_events, UNIQUE en backend/src/index.ts:3080-3088); backend/src/routes/stripe-webhook.ts:20-23 (idéntico con stripe_webhook_events, PK event_id en index.ts:989-990); backend/src/lib/orderFulfillment.ts:21,32-41 (finalizePaidOrder relee status con FOR UPDATE dentro de la tx, protegiendo contra webhook+POST /:id/sync-mp concurrentes); test puro en backend/scripts/test-mercadopago.ts:39-57. |
| E4 | ⚠️ | 🔴 | El mecanismo existe pero no cubre el escenario real: (1) requiere que `orders.mp_payment_id` YA esté seteado, y ese campo SOLO lo escribe el webhook (mercadopago-webhook.ts:74) — si el webhook nunca llegó, mp_payment_id sigue NULL y sync-mp responde 400 'La orden no tiene un pago... que reconciliar' (orders.ts:775-777); no existe llamada a `/v1/payments/search?external_reference=` en lib/mercadopago.ts para localizar el pago sin conocer su ID. (2) No hay UI: frontend/src/pages/admin/orders/OrdersVerification.tsx:333-354 para órdenes con tarjeta SOLO ofrece el botón 'Cancelar' (ver línea 341-353); sync-mp no se invoca desde ningún .tsx del repo (grep sin resultados) — si el cliente ya pagó y el webhook no llegó, la única acción visible para recepción/dueña es cancelar la orden de una clienta que sí pagó. (3) No hay cron proactivo: services/cron-jobs.ts solo expira pending_payment de transferencia tras 48h (líneas 382-397), sin re-chequeo contra la API de MP para tarjeta. Debería: agregar botón 'Reconciliar con MP' en OrdersVerification.tsx que llame sync-mp, extender la búsqueda por external_reference cuando falte mp_payment_id, y un cron horario sobre pending_payment+card. |
| E5 | ⚠️ | 🟠 | No hay 'mensaje claro con motivo genérico' de por qué falló: igual que en E1, el webhook nunca marca status='rejected' para tarjeta (solo rejected_at, mercadopago-webhook.ts:83-88), y OrderDetail.tsx no lee `?mp=failure` ni expone mp_status_detail al cliente. El único banner de rechazo visible (OrderDetail.tsx:696-710, 'Pago rechazado' / 'tu comprobante no pudo ser validado') está redactado para transferencias rechazadas por el admin, no para declines de tarjeta — un cliente con tarjeta rechazada solo ve 'Esperando pago' indefinidamente. Debería mapear rejected/cancelled a status='rejected' y mostrar un mensaje específico de tarjeta rechazada con CTA a reintentar/cambiar método, en frontend/src/pages/client/OrderDetail.tsx. |
| E6 | ⚠️ | 🟠 | Es solo contable, nunca ejecuta el reembolso real: `refundCharge` (backend/src/lib/stripe.ts:106-112) está definida pero jamás invocada desde ninguna ruta (grep sin resultados fuera de su propia definición), y no existe ninguna llamada al endpoint de reembolsos de MercadoPago en todo el repo. La dueña debe reembolsar el dinero manualmente fuera del sistema (terminal, dashboard MP/Stripe, transferencia) y luego reflejarlo con este botón. Además es todo-o-nada: no hay cálculo de reembolso parcial por clases usadas vs. no usadas (solo un boolean que marca TODOS los payments 'completed' de la membresía como 'refunded' por el monto íntegro). Tampoco inserta una entrada en `admin_actions`: logAction no se llama en este bloque (memberships.ts:978-1068), a diferencia de otras acciones administrativas del mismo archivo (líneas 527, 688, 937, 1221, 1346) — sin trazabilidad formal de auditoría para I8. Debería: integrar refundCharge/refund API de MP para reembolsos con tarjeta, soportar monto parcial, y agregar logAction en el cancel-con-refund. |
| E7 | ⚠️ | 🟡 | El evento se detecta y el paquete se congela correctamente, pero 'alertar a la dueña' no ocurre: charge.dispute.created solo hace `console.warn` (stripe-webhook.ts:80), y ninguna rama de ninguno de los dos webhooks llama a lib/whatsapp.ts, lib/web-push.ts ni services/email.ts para notificar a dueña/staff; reversePaymentByReference (orderFulfillment.ts) tampoco dispara notificación alguna. Además `payments.status` queda igual ('refunded') sea reembolso normal o contracargo — la única distinción vive en el texto libre `cancellation_reason` de la membresía, sin vista/filtro dedicado de contracargos. MercadoPago tampoco tiene fase intermedia de 'disputa abierta' en este código (solo el estado final charged_back), así que ese proveedor no da ningún aviso temprano. Debería: agregar una notificación real (push/WhatsApp/email a la dueña) en charge.dispute.created y en la rama charged_back de mercadopago-webhook.ts, y marcar el payment con un status distinto ('charged_back') para poder filtrarlo en reportes. |
| E8 | ⚠️ | 🟡 | Esos mensajes confirman la ACTIVACIÓN DE LA MEMBRESÍA (plan, clases incluidas, vigencia) — no son un comprobante/recibo de PAGO: no incluyen el monto pagado, el método, ni un folio/referencia de transacción. No existe ninguna plantilla de 'recibo' o 'receipt' en services/email.ts ni lib/whatsapp.ts (grep sin resultados para 'recibo'/'receipt'/'comprobante de pago'). Debería añadirse un bloque de recibo (monto+método+folio) en el mismo mensaje o uno adicional, en los mismos 3 puntos de disparo ya identificados. |
| E9 | ❌ | ⚪ | No hay campo para capturar datos fiscales (RFC, razón social, uso CFDI) ni un endpoint/tabla para solicitudes de factura, ni aviso a la dueña cuando alguien la pide. La página pública promete 'Facturación disponible' sin que exista el flujo detrás. Debería vivir como un nuevo endpoint (p.ej. POST /orders/:id/invoice-request) + tabla invoice_requests + notificación a la dueña (email/WhatsApp); la emisión en sí puede seguir siendo externa/manual. |
| E10 | ➖ | 🟠 | backend/src/lib/commission.ts y backend/src/routes/commissions.ts implementan comisión de VENTAS para personal/nómina (J5), no split payments de pasarela. Sin application_fee, sin OAuth de MercadoPago por tenant, sin tabla de credenciales cifradas por estudio (grep vacío para application_fee/marketplace/platform_fee/oauth mercadopago). Consistente con el perfil: Casa Shé opera una sola sucursal propia, no es una plataforma multi-tenant que cobra comisión a otros estudios. |

## F — Check-in y asistencia

| ID | Estado | Sev. | Evidencia / Gap |
|---|---|---|---|
| F1 | ✅ | 🔴 | El 'saldo' y la 'vigencia' de la membresía NO se re-validan en el momento del escaneo: se validaron y consumieron al reservar (política documentada de Casa Shé: descuento al reservar, ver F3/bookings.ts:686-788), así que si una membresía fuese congelada/cancelada por admin DESPUÉS de la reserva pero antes de la clase, el check-in igual la aceptaría. Es un edge case raro, no bloqueante; la latencia <2s no es verificable estáticamente (no_verificable en cuanto a esa métrica puntual) pero la ruta es un solo round-trip de queries simples, consistente con el objetivo. |
| F2 | ✅ | 🔴 | No existe una caja de búsqueda libre por nombre/teléfono/email que lleve DIRECTO a marcar check-in (el buscador nombre/email/teléfono que sí existe en BookingsScreen.tsx:317-339 y ScanScreen.tsx sirve para CREAR una reserva nueva o ver la ficha de lealtad, no para localizar una reserva existente y hacerle check-in). En la práctica el roster por clase/día cubre el mismo caso de uso (clienta sin celular) sin necesitar el teléfono. |
| F3 | ✅ | 🔴 | backend/src/routes/bookings.ts:766-788 y :1000-1010 descuentan 1 crédito EXACTAMENTE en el momento de reservar (política explícita del estudio), con decremento auto-guardado (`UPDATE ... SET col = col - 1 WHERE id=$1 AND col > 0`, rowCount=0 aborta con 403) para cerrar la carrera de doble-reserva. La regla D9 (qué membresía se consume primero) es determinista y centralizada en backend/src/lib/membershipSelection.ts:70-97 (pickBestMembership: acotada antes que ilimitada → vigencia más próxima → creada más antigua), usada tanto en la reserva de cliente como en admin-book. Las 3 rutas de check-in (qr/self/manual, checkin.ts) NUNCA tocan reformer_remaining/multi_remaining — solo las LEEN para notificar al wallet (líneas 386-398, 574-585, 712-723) — así que no hay riesgo de doble descuento en el check-in sin importar el método. |
| F4 | ✅ | 🟠 | El flujo cruza 2-3 componentes (diálogo de alta + diálogo de venta + fila de booking) dentro del mismo drawer en vez de ser una única pantalla/formulario; es un detalle de UX menor, no bloquea el caso de uso. |
| F5 | ⚠️ | 🟡 | Hoy: se calcula un flag de tardanza con umbral fijo que no se muestra a nadie ni dispara ninguna acción (el lugar no se libera a la lista de espera ni se avisa a recepción). Debería: exponer un `checkin_settings.late_tolerance_minutes` editable en Ajustes (routes/settings.ts, generic PUT /:key ya existe como mecanismo) y mostrar/usar el flag en CheckinScreen.tsx/BookingsScreen.tsx (badge de 'llegó tarde' y, opcionalmente, acción de liberar el lugar tras X minutos). |
| F6 | ✅ | 🟠 | El modelo usa un tope fijo interno (12+12) definido por el estudio en vez de validar cada check-in contra el cupo/entitlement real que reporta la plataforma agregadora por visita — es una aproximación de 'soft allocation', suficiente para no tocar paquetes propios y para conciliar reportes, pero no una integración real con TotalPass/Wellhub/Fitpass. |
| F7 | ❌ | 🟡 | Hoy: si se cae internet o la luz, recepción no tiene ninguna lista imprimible ni forma de capturar asistencias localmente para sincronizar después sin arriesgar duplicados. Debería: una vista imprimible/exportable del roster del día (frontend/src/pages/reception/CheckinScreen.tsx o reception-dashboard checkin-agenda) y, idealmente, un cache local (IndexedDB/localStorage) con cola de check-ins pendientes que se sincronice contra /api/checkin/manual respetando el guard de 'ya hiciste check-in' al reconectar. |
| F8 | ✅ | 🔴 | Detalle menor no bloqueante: el UPDATE de `bookings.status` en las 3 rutas no lleva una cláusula `WHERE status <> 'checked_in'` (solo `WHERE id = $1`), así que dos escaneos verdaderamente simultáneos (mismo instante, dos dispositivos) podrían ambos pasar el chequeo inicial antes de que el otro escriba, generando un checkin_logs duplicado — no afecta créditos ni es el escenario real de 'doble escaneo' (secuencial), que sí queda cubierto. |
| F9 | ⚠️ | 🟡 | Hoy: la instructora ve su lista de reservadas antes de clase y su historial de asistencia después, pero no una vista EN VIVO de quién ya llegó durante la clase misma. Debería: en frontend/src/pages/coach/ClassDetail.tsx, o bien consumir GET /api/checkin/class/:classId (ya autorizado para su rol) o renderizar el campo `checked_in_at` que el endpoint que ya usa sí entrega, con refresco periódico mientras la clase está en curso. |

## G — Wallet passes (Apple/Google)

| ID | Estado | Sev. | Evidencia / Gap |
|---|---|---|---|
| G1 | ⚠️ | 🟠 | Hoy: el pass NO se genera ni se entrega automáticamente al activar (online o manual en recepción) — la clienta debe descubrir por sí misma la pantalla /app/wallet y tocar el botón. No existe pantalla de éxito post-pago ni WhatsApp con el link de descarga (el canal está apagado). Debería: agregar un CTA 'Agregar a tu Wallet' en el correo de activación (services/email.ts sendMembershipActivatedEmail) y/o en la pantalla de orden/checkout (frontend/src/pages/client/OrderDetail.tsx), y si se reactiva WhatsApp transaccional, incluir el link a /app/wallet en sendMembershipActivatedNotice. |
| G2 | ✅ | 🟠 | backend/src/lib/apple-wallet.ts:58-69 PLAN_STYLES (paleta dorado Casa Shé por tier: básico/intro/premium/ilimitado) y backend/src/lib/google-wallet.ts:54-81 PLAN_STYLES con la misma paleta y tiers; ambos exponen el mismo contenido — nombre, clases restantes, puntos, vigencia, miembro desde, próximo evento (apple-wallet.ts:381-405, google-wallet.ts:452-488); frontend/src/pages/client/Wallet.tsx:272-326 ofrece los dos botones (Apple y Google) en la misma pantalla condicionados a `canDownloadPass`. |
| G3 | ⚠️ | 🟠 | Hoy: un ajuste manual de créditos o vigencia (I5), una pausa/reanudación (D7), un canje de recompensa o un bono automático de lealtad cambian el saldo/vigencia real en BD pero el pass YA instalado en el teléfono de la clienta se queda mostrando el dato viejo hasta que ocurra una acción no relacionada (reservar, check-in, renovar) que sí dispare el refresh. Debería: agregar la llamada a notifyAllUserDevices/recordPassUpdate + upsertGoogleLoyaltyObject en memberships.ts (pause/resume/dates/credits) y en loyalty.ts (redeem) y en cron-jobs.ts (bonos), o como mínimo encolar esas membresías para el POST /api/wallet/admin/refresh-passes existente (routes/wallet.ts:579-726). |
| G4 | ✅ | 🟡 | backend/src/lib/notifications.ts:206-270 notifyApple → sendAPNsAlertNotification (alerta visible) + notifyAllDevices (push silenciosa que fuerza refetch del pase); :272-312 notifyGoogle → sendGoogleWalletMessage (addMessage visible en Google Wallet). Se dispara desde notifyMembershipExpiring (backend/src/services/cron-jobs.ts:285, alimentando D4/H5), notifyMembershipRenewed, notifyClassAttended y sendCustomNotification (routes/wallet.ts:743-768, admin manual). |
| G5 | ⚠️ | 🟡 | Hoy: la re-descarga solo funciona si la clienta tiene la app y su sesión activa; no existe comando de WhatsApp ni botón en el panel de recepción/ficha de cliente para reenviar el link del pass a alguien que perdió acceso a la app o es menos técnica. Debería: agregar una acción 'Reenviar pase por WhatsApp' en la ficha de cliente (frontend/src/pages/reception/ScanScreen.tsx o el perfil admin) que llame a /wallet/pass/apple\|google en nombre del staff y mande el link. |
| G6 | ⚠️ | 🟡 | Hoy: cuando el cron marca una membresía como vencida, el pass ya instalado (Apple o Google) NO recibe ningún push — se queda mostrando el saldo/plan de antes de vencer indefinidamente, hasta que otra acción no relacionada lo refresque. Y aunque se regenere, Apple no comunica visualmente 'vencido' (solo una fecha pasada en letra chica), sin CTA de renovar. Debería: en markExpiredMemberships (cron-jobs.ts) iterar las recién vencidas y disparar el refresh de wallet (o encolarlas para /api/wallet/admin/refresh-passes), y agregar en el storeCard de Apple una leyenda tipo 'VENCIDO · Renueva en la app' cuando end_date < hoy. |
| G7 | ✅ | 🟡 | frontend/src/components/wallet/WalletQr.tsx renderiza el QR firmado (GET /wallet/pass → qrPayload) disponible para cualquier cliente, tenga o no wallet nativo. backend/src/routes/wallet.ts:249-305 POST /wallet/lookup acepta indistintamente qrPayload firmado, membershipId (el mismo valor crudo que llevan los barcodes nativos: apple-wallet.ts:410 `message: m.id`, google-wallet.ts:432-436 `barcode.value: membership.id`) o userId. frontend/src/pages/reception/ScanScreen.tsx:81,158-163 detecta con un regex si el código escaneado es un UUID (pass nativo) o el payload firmado (fallback web) y llama al mismo endpoint en ambos casos — misma validez para el único escáner del producto. |

## H — Notificaciones y comunicación

| ID | Estado | Sev. | Evidencia / Gap |
|---|---|---|---|
| H1 | ⚠️ | 🟠 | Hoy: la confirmación llega por email+push+wallet (WhatsApp apagado a propósito, política 2026-06-23). Ninguno de esos canales incluye el número de cama/reformer asignado, aunque el sistema sí guarda esa asignación (tabla booking_reformers, ver backend/src/routes/reformers.ts) — sendBookingConfirmationEmail (email.ts:608) no recibe ese dato. Debería: agregar el spot/reformer number al payload de la confirmación (email y push). |
| H2 | ⚠️ | 🟠 | Hoy: NINGÚN cliente recibe recordatorio 24h/2h antes de su clase, por ningún canal — el comentario en cron-jobs.ts:932-935 dice que se apagó durante la transición de Fitune ('sendClassReminders() queda disponible para reactivar cuando se corte Fitune'). Falta: descomentar cron.schedule (cron-jobs.ts:936-937) o exponerlo en routes/cron.ts para trigger manual cuando aplique. Nota: incluso reactivado, el envío por WhatsApp del recordatorio queda deshabilitado dentro de la misma función (cron-jobs.ts:804-805), quedando solo in-app+push. |
| H3 | ⚠️ | 🔴 | El único aviso al cliente afectado es un push del navegador/OS; no usa writeInAppNotification (no aparece en la campanita in-app/tabla notifications), no hay email de respaldo al cliente, y WhatsApp está desactivado. Si el cliente no tiene push habilitado (común en iPhone sin 'añadir a inicio'), se entera de CERO de que su clase fue cancelada — solo notaría el crédito devuelto si entra a revisar 'mis reservas'. Tampoco se sugieren horarios alternativos. Fix: en lib/cancel-class.ts, además del push, llamar writeInAppNotification (backend/src/lib/in-app-notifications.ts) y considerar email de respaldo vía services/email.ts. |
| H4 | ⚠️ | 🟠 | No existe una 'ventana de confirmación explícita' (p.ej. 30 min para confirmar antes de pasar a la siguiente persona) — el sistema auto-confirma y cobra el crédito directamente sin dar oportunidad de aceptar/rechazar. Si el cliente ya no puede ir debe cancelar activamente (eso sí re-libera el lugar y re-dispara la promoción), pero no hay temporizador que lo salte automáticamente a la siguiente persona si no responde. Requeriría un estado intermedio 'offered' con expiración en bookings, en vez del salto directo a 'confirmed' que hace lib/waitlist.ts:222. |
| H5 | ⚠️ | 🟠 | daysToNotify está hardcodeado a `[1]` (cron-jobs.ts:248, comentario 'Notificar SOLO cuando queda 1 día para vencer') — no configurable desde admin y no dispara por 'clases restantes' (p.ej. 2 clases), solo por fecha de vencimiento a 1 día. El mensaje tampoco incluye el saldo de clases restantes ni un link/CTA directo a renovación (lib/whatsapp.ts:327-334 solo da nombre de plan, fecha y días restantes). Fix viviría en system_settings (nueva key configurable de días) + agregar saldo/CTA al template. |
| H6 | ⚠️ | 🟡 | El contenido de bienvenida es solo credenciales (email + contraseña temporal) en ambos canales — no incluye 'cómo reservar', políticas clave del estudio, ni el link/QR de su wallet pass, como pide el escenario. Fix: enriquecer el template (services/email.ts sendClientWelcomeEmail y lib/whatsapp.ts sendClientWelcome) con esos 3 puntos. |
| H7 | ❌ | ⚪ | No existe ningún mecanismo de win-back. Debería vivir como nuevo job en backend/src/services/cron-jobs.ts que detecte usuarios con membresía activa sin check-in en N días (2-3 semanas) y dispare un WhatsApp/email respetuoso y espaciado (con control de frecuencia para no reincidir), aprovechando el toggle receive_promotions que ya existe en el perfil (frontend/src/pages/client/ProfilePreferences.tsx) pero que hoy no filtra nada porque no hay envíos de marketing. |
| H8 | ⚠️ | ⚪ | birthdayBonus/anniversaryBonus (cron-jobs.ts) NUNCA llaman a notifyPointsEarnedExternal — grep confirma que esa función solo se invoca desde orderFulfillment.ts, routes/memberships.ts, routes/users.ts, routes/auth.ts y routes/orders.ts, nunca desde cron-jobs.ts. El bono se acredita en silencio: el cliente no recibe ningún WhatsApp/email/push felicitándola — solo lo notaría si entra a revisar su saldo de puntos. Fix trivial: agregar `void notifyPointsEarnedExternal(u.id, points, 'birthday')` (y análogo 'anniversary') dentro de los loops en cron-jobs.ts:577-593 y 638-654, ya que el resto de la plumbing (templates, reason labels) ya existe. |
| H9 | ⚠️ | 🟡 | De los 3 toggles, solo receive_reminders se consulta realmente antes de enviar algo (y ese envío está apagado, ver H2). receive_promotions y receive_weekly_summary se guardan en base de datos pero ningún flujo del backend los lee — no existe H7 (win-back) ni ningún job de 'resumen semanal' que los respete. No es un bug de fuga de datos, pero son preferencias sin efecto real hasta que existan esas features; documentar para no asumir cobertura completa. |
| H10 | ❌ | 🟡 | Si un número de WhatsApp es inválido o el mensaje no llega, nadie se entera — ni el perfil del cliente lo refleja ni hay alerta en admin/recepción. Falta: (1) persistir el evento de handleMessageUpdate ligado al mensaje original (requiere guardar el message-id al enviar en sendWhatsAppMessage, lib/whatsapp.ts:35-55), (2) marcar un flag en users (p.ej. whatsapp_delivery_failed) cuando status=0/error, y (3) superficie en el panel de recepción/admin (routes/reception-dashboard.ts o routes/admin.ts) para poder detectarlo y contactar por otro canal. |

## I — Panel admin y operación

| ID | Estado | Sev. | Evidencia / Gap |
|---|---|---|---|
| I1 | ✅ | 🔴 | CRUD completo por entidad: tipos de clase en backend/src/routes/class-types.ts:29,57,98,177 (GET/POST/PUT/DELETE); horarios (plantilla semanal day_of_week + excepción specific_date) en backend/src/routes/schedules.ts:28,73,113,158 con validación de capacidad por categoría (lib/schedule.ts capacityError); instructoras en backend/src/routes/instructors.ts (POST 236, PUT 551, DELETE 700, disponibilidad 992, generar acceso 815); sucursales en backend/src/routes/facilities.ts:8,49,68,91,129; camas/reformers numeradas + editor de layout en backend/src/routes/reformers.ts:176,197,237 y frontend/src/pages/admin/facilities/FacilityLayoutEditor.tsx. Frontend: frontend/src/pages/admin/classes/ClassTypesList.tsx, schedules/WeeklySchedule.tsx, staff/InstructorsList.tsx, facilities/FacilitiesList.tsx. |
| I2 | ⚠️ | 🟠 | Hoy: el endpoint consolidado de 'vista hoy' no incluye pagos pendientes de confirmar (transferencias con comprobante subido); esa cola vive en una pantalla aparte (frontend/src/pages/reception/ApprovalsScreen.tsx vía PendingMembershipsPanel, ligada a GET /api/orders/pending en backend/src/routes/orders.ts:240) accesible solo a recepción elevada (useIsElevated). Debería: sumar un contador/badge de aprobaciones pendientes al payload de GET /reception/dashboard (backend/src/routes/reception-dashboard.ts) para que cualquier recepcionista lo vea al abrir el día, sin depender de navegar a otra sección ni de tener rol master. |
| I3 | ⚠️ | 🔴 | Hoy: ni full-profile (backend/src/routes/admin.ts:153-246) ni la bitácora (backend/src/routes/clients.ts:13-159) exponen el historial de PAGOS reales (monto, método, referencia, fecha de cada transacción) — full-profile solo trae el precio de lista del plan (p.price as price_paid), no filas de la tabla payments; ClientDetail.tsx no tiene pestaña 'Pagos'. Recepción debe ir a la pantalla global de pagos (backend/src/routes/payments.ts GET /transactions) y buscar manualmente por cliente. Debería: agregar `SELECT * FROM payments WHERE user_id = $1` al full-profile (o una pestaña dedicada) para que 'saldo, vigencia, historial de clases y pagos' vivan en un solo lugar como pide el escenario. |
| I4 | ✅ | 🔴 | Un solo flujo en frontend/src/pages/reception/ClientsScreen.tsx: NewClientDialog (línea 98, POST /api/users) para clienta nueva + SellPlanDialog (línea 174) para vender/asignar plan a cualquier clienta de la lista (línea 1054). Backend: POST /api/memberships/assign (backend/src/routes/memberships.ts:367) crea/activa la membresía y registra el pago (método, monto, referencia) en la misma transacción (memberships.ts ~455-475 INSERT INTO payments), cubriendo D8/E2 en una sola pantalla. |
| I5 | ⚠️ | 🟠 | Hoy: admin/super_admin y recepción con 'creditos_sin_limite' pueden ajustar créditos SIN motivo alguno (memberships.ts:1290 `if (!sinLimite) {...}` — el bloque de validación de motivo se salta por completo); el ajuste de VIGENCIA (fechas) no exige motivo para ningún rol, nunca. Debería: mover la validación de motivo obligatorio (mínimo N caracteres) fuera del `if (!sinLimite)` para que aplique a cualquier rol en ambos endpoints (backend/src/routes/memberships.ts PATCH /:id/credits y PATCH /:id/dates), tal como pide I5/D12 ('motivo obligatorio' sin excepción). |
| I6 | ⚠️ | 🟠 | Hoy: tanto el schema (ClosedDaySchema en closed-days.ts:9-12, un solo campo `date`) como el formulario (frontend/src/pages/admin/settings/ClosedDays.tsx, un único input type=date) solo aceptan UN día por alta; para bloquear un rango (ej. vacaciones de una semana) la dueña debe repetir el alta día por día. Debería: aceptar startDate/endDate en el POST y expandir el rango server-side (backend/src/routes/closed-days.ts) + un selector de rango en el frontend, tal como pide I6 ('días o rangos completos de una vez'). |
| I7 | ✅ | 🟠 | backend/src/lib/permissions.ts define PERMISSION_KEYS granulares, ADMIN_ONLY_KEYS y validatePermissionChange (candado anti-escalación: recepción no se auto-edita, no otorga lo que no tiene, no otorga claves admin-only). Middleware backend/src/middleware/requirePermission.ts aplica esto por ruta. Las rutas financieras (backend/src/routes/reports.ts, backend/src/routes/stats.ts) usan requireRole('admin'/'super_admin') directo — recepción queda bloqueada de finanzas sin importar sus permisos otorgados. UI completa de gestión de equipo en frontend/src/pages/admin/reception/ReceptionStaffList.tsx (toggle Master solo por admin, editor de permisos por clave). |
| I8 | ⚠️ | 🟠 | Hoy hay dos huecos concretos en el propio checklist de I8: (1) POST /api/memberships/:id/cancel con refund=true (backend/src/routes/memberships.ts:978-1058) — el reembolso de pagos y reversión de puntos NUNCA llama logAction; no queda registro de quién reembolsó, cuánto ni por qué. (2) La cancelación masiva de clases NO queda en admin_actions salvo cuando se dispara con scope='series' (classes.ts:997); ni la cancelación individual (DELETE /api/classes/:id sin scope, líneas 962-1064) ni la cancelación masiva por día cerrado/festivo (backend/src/routes/closed-days.ts:49-96, que llama cancelClassWithRefunds sin loguear) generan entrada de auditoría. Debería: agregar logAction() en el POST /:id/cancel con refund=true y en cancelClassWithRefunds/closed-days.ts, para que 'reembolsos' y 'cancelaciones masivas' — mencionados explícitamente en I8 — queden siempre en el log. |
| I9 | ⚠️ | 🟡 | Hoy: no existe ninguna pantalla ni endpoint para editar 'tolerancia' (minutos de gracia para llegar tarde) ni 'penalizaciones' por no-show/strikes — el único rastro es un booleano `no_show_penalty` en los defaults de backend/src/lib/settings.ts:35,106 que NUNCA se lee en ningún flujo de check-in/cron (grep sobre backend/src no encuentra ninguna lectura de ese flag fuera de su propia definición y el seed de backend/src/index.ts:1077-1081); es una bandera muerta, no una política funcional. Debería: implementar la lógica de tolerancia/penalización en el cron de no-shows (backend/src/services/cron-jobs.ts) leyendo un setting real, y exponer su edición en un panel (p.ej. ampliar frontend/src/pages/admin/settings/PoliciesSettings.tsx). |
| I10 | ✅ | 🟡 | Arquitectura multi-sucursal real: facility_id en schedules (backend/src/routes/schedules.ts), classes, sales, payments, cash_shifts; resolveRequestFacility (backend/src/lib/requestFacility.ts) acota a recepción por sucursal salvo permiso 'multi_sucursal'; reportes consolidados y por sucursal en backend/src/routes/reports.ts (p.ej. /revenue línea 273, /profit-by-facility línea 661 con facility_id=null→consolidado o filtrado); CRUD de sucursales en backend/src/routes/facilities.ts y frontend/src/pages/admin/facilities/FacilitiesList.tsx. |
| I11 | ✅ | 🟠 | backend/src/routes/clients.ts GET /:id/bitacora (líneas 37-152) agrupa por periodo de membresía: asistencias con booked_by/checked_in_by/consumed_category, cancelaciones con cancelled_by/motivo/consumed_category, y ajustes_creditos (old_data/new_data/admin_name) — responde exactamente 'qué reserva/check-in consumió el crédito, de qué paquete (reformer/multi) y cuándo'. Se refuerza con bookings.consumed_category persistido por reserva (visto en cancel-class.ts:63-70 al reembolsar al bucket correcto) y se renderiza en frontend/src/components/bitacora/ClientBitacora.tsx. |

## J — Instructoras

| ID | Estado | Sev. | Evidencia / Gap |
|---|---|---|---|
| J1 | ✅ | 🟠 | backend/src/routes/instructors.ts:1106-1157 (GET /:id/classes con params date/from/to, incluye max_capacity, current_bookings, waitlist_count); frontend/src/pages/coach/Schedule.tsx:45-79 (vista semanal Lun-Dom vía from/to) y frontend/src/pages/coach/Dashboard.tsx:97-109,204,306-318 (vista 'hoy' + % ocupación agregada y por clase, alerta de llena/waitlist) |
| J2 | ⚠️ | 🟠 | El indicador de 'primera vez' (columna is_first_class, ya usada en routes/checkin.ts:899-964 y routes/reception-dashboard.ts:223-269 para el panel de recepción) NUNCA se incluye en la query de asistentes que consume la instructora (routes/instructors.ts:1181-1206 / routes/classes.ts:1240-1261) ni se renderiza en frontend/src/pages/coach/ClassDetail.tsx o Students.tsx. La instructora ve alertas de salud pero no sabe quién es nueva en su clase. Fix: agregar `cl.is_first_class` (join con classes existente en checkin.ts) al SELECT de attendees en ambos routers y mostrar badge 'Primera vez' en ClassDetail.tsx. |
| J3 | ✅ | 🟡 | backend/src/routes/instructors.ts:1221-1282 (POST /:id/classes/:classId/checkin, soporta check-in masivo `all=true` o por bookingIds individuales, solo sobre bookings 'confirmed'→'checked_in', dispara awardCheckinPoints) |
| J4 | ⚠️ | 🟡 | Las 3 rutas notifican a las DOS instructoras (saliente/entrante) pero ninguna notifica a las CLIENTAS ya reservadas en la clase — routes/classes.ts:1138-1144 obtiene `bookedClients` pero el comentario en línea 1205 dice 'se notifican aparte por el flujo de class_cancelled/updated' y ese flujo no existe (no hay plantilla de WhatsApp para cambio de instructora en lib/whatsapp.ts, que solo tiene sendBookingConfirmation/sendCancellationNotice/etc., líneas 158-317); y change-instructor (classes.ts:868) declara explícitamente 'NO notifica a nadie'. Esto es exactamente el hueco que el escenario C6 de la matriz señala. Fix: agregar notificación WhatsApp/push a los bookings 'confirmed'/'waitlist' de la clase en los 3 endpoints. |
| J5 | ✅ | ⚪ | backend/src/routes/coach-payroll.ts: GET / (líneas 51-125, conteo de clases 'completed' con ≥1 reserva real por periodo biweekly/monthly, tarifa pay_rate_per_class, total); POST /payouts (204-303, congela snapshot + genera egreso de nómina en transacción atómica); GET /me (345-393, coach ve su propia nómina proyectada y pagada); frontend/src/pages/coach/Earnings.tsx consume /me |

## K — Reportes y métricas

| ID | Estado | Sev. | Evidencia / Gap |
|---|---|---|---|
| K1 | ✅ | 🟠 | backend/src/routes/reports.ts:273-404 (GET /revenue: daily, byMethod L318-327, byPlan L330-348, byFacility L362-371, total L350-359) y :12-182 (/overview). Todas las queries filtran pay.status='completed' (PAY_WHERE, L292-293) sobre COALESCE(completed_at, created_at) anclado a America/Mexico_City. El reembolso/cancelación de membresía transiciona el pago a status='refunded' (routes/memberships.ts:1002-1057, en particular UPDATE...SET status='refunded' L1041; también mercadopago-webhook.ts:89-96 para refund/chargeback y orders.ts:1233-1253 para reembolso de orden), lo que lo excluye automáticamente de estas queries de ingresos (son queries en vivo sobre el status actual, no snapshots). |
| K2 | ✅ | 🟠 | backend/src/routes/reports.ts:187-268 (GET /classes: byType por class_type L195-204, byDayOfWeek L207-215, byTime L218-226, occupancyRate L229-240, classesByStudio por sucursal L242-255) y :536-590 (GET /instructors: total_classes, avg_attendance, avg_occupancy por instructor). Frontend wired en frontend/src/pages/admin/reports/ReportsClasses.tsx y ReportsInstructors.tsx. |
| K3 | ✅ | 🟠 | activas/inactivas y nuevas: routes/reports.ts:12-182 (/overview: activeMembers, newMembers para el periodo) y frontend/src/pages/admin/clients/ClientsList.tsx:273-294 (pills con conteos Activas/Vencidas/Sin plan, filtrables). Renovación: routes/reports.ts:409-506 (/retention: renewalRate) y :810-847 (/renewal-by-plan, desglosado por plan). 'Clientas que no renovaron': ClientsList con membershipStatus='expired' (routes/users.ts:1124-1130, vía LATERAL join que prioriza la membresía activa/pendiente sobre la vencida, L1074-1085) da la lista de clientes cuya membresía vigente actual es expired/cancelled. |
| K4 | ⚠️ | 🟠 | El botón 'Recordar' de la lista (línea 115-119) NO envía nada: onClick solo dispara `toast({ title: 'Notificación enviada' })`, sin llamada a la API de WhatsApp/push/email — es un mock, la clienta nunca recibe el recordatorio desde este reporte. Además la ventana de 7 días está hardcodeada en el frontend (no configurable) y el reporte solo mira end_date (paquetes por fecha); no contempla paquetes de crédito (reformer_remaining/multi_remaining) que se están agotando pero con end_date lejano. Fix: conectar el botón a un endpoint real de notificación (p.ej. POST /api/notifications/membership-reminder) y sumar un criterio de 'créditos casi agotados' además de fecha, en el mismo backend/src/routes/reports.ts o uno nuevo /reports/expiring. |
| K5 | ⚠️ | 🟡 | Las cadenas reales que escribe el flujo normal de cancelación de clienta (función SQL cancel_booking(), backend/src/index.ts:1966-1974) son 'Cancelada (con devolución)' / 'Cancelada (sin devolución)' — ninguna contiene 'menos de' ni 'admin'. Resultado: late_cancellations siempre da ~0 (nunca matchea) y early_cancellations/'Reposiciones' cuenta erróneamente TODAS las cancelaciones (incluidas las tardías sin devolución) como si fueran anticipadas. Además, no_shows cuenta bookings con status='confirmed' y clase pasada, pero el cron markNoShows (services/cron-jobs.ts:415-432, cada 30 min) los pasa a status='no_show' — la query nunca busca ese status, así que en cuanto ENABLE_CRON_JOBS esté encendido (hoy está OFF según auditoría previa) el conteo de no-shows caerá a 0 silenciosamente para cualquier clase de más de 30 min de antigüedad. Por último, el botón 'Contactar' sobre riskyUsers (frontend/src/pages/admin/reports/ReportsRetention.tsx:246-249) no tiene onClick — es decorativo, no hace nada. Fix: alinear los ILIKE de reports.ts con las cadenas reales de cancel_booking()/cancelClassWithRefunds (o mejor, agregar una columna estructurada cancellation_type), cambiar el filtro de no_shows a status='no_show' OR (confirmed AND date<NOW()), y conectar 'Contactar' a un canal real (WhatsApp/llamada). |
| K6 | ✅ | 🟡 | backend/src/routes/reports.ts:508-531 (GET /platforms: activos, reservas y checkins por plan interno Totalpass/Wellhub/Fitpass) alimentado por planes seedeados con is_internal=true (backend/src/index.ts:2821-2847, asignación manual por recepción/admin — sin precio, créditos ilimitados/topados a 12+12, identificados por color). Frontend en pages/admin/reports/ReportsRetention.tsx:266-292 ('Plataformas: alumnos activos/reservas/asistencias', con nota explícita de que las asistencias son lo que se factura a cada plataforma). Es reconciliación manual (no hay feed automático de TotalPass/Wellhub/Fitpass), consistente con el perfil operativo declarado. |
| K7 | ⚠️ | 🟡 | El único export CSV de todo el admin (`grep -rl csv pages/admin`) es el resumen agregado de /overview (ingresos, egresos, miembros activos, tendencia mensual) — NO existe export CSV de listados fila-por-fila de clientas (ClientsList.tsx no tiene exportar), pagos (PaymentsReports.tsx no tiene exportar) ni asistencias/bookings. Si la dueña quiere 'llevarse los datos' de clientas/pagos/asistencias para Excel/contador, hoy no puede. Fix: agregar botón de exportar CSV en ClientsList, PaymentsReports y un listado de bookings/asistencias, reusando el patrón BOM+blob ya probado en ReportsOverview.tsx. |

## L — Seguridad, legal y datos

| ID | Estado | Sev. | Evidencia / Gap |
|---|---|---|---|
| L1 | ➖ | 🔴 | Casa Shé es single-tenant: no existe concepto tenant_id/studio_id en el schema (grep en database/schema.sql y schema_complete.sql sin resultados); `facilities` son sucursales de UN mismo negocio, no distintos clientes-estudio (routes/facilities.ts, lib/requestFacility.ts). No hay un 'estudio B' del que aislar datos. Sí se verificó IDOR entre clientas en los endpoints muestreados: bookings.ts:1298 (GET /:id exige dueño o admin y oculta email si no es admin), classes.ts:1222-1238 e instructors.ts:1178-1188 (un instructor solo ve asistentes/health_notes de SUS clases, con comentario explícito de un fix previo en instructors.ts:28-31 'podía leer datos de OTRO coach'), users.ts:865-895 y :900-921 (perfil propio o admin), orders.ts:431-476 (orden propia o admin), wallet.ts:315-328 (pase Apple solo si la membership es del usuario). Además se confirmó middleware/auth.ts:48-61: `authenticate` revalida `users.is_active` Y `instructors.is_active` en cada request (JOIN con bool_and), cerrando el hueco de staff dado de baja que conservaba el JWT válido. |
| L2 | ⚠️ | 🟠 | Hoy solo hay un consentimiento GENERAL (aceptar términos) capturado en el registro; el cuestionario de salud real (routes/onboarding.ts POST /submit, SubmitSchema) no pide un consentimiento EXPRESO Y SEPARADO en el momento de capturar el dato sensible, que es lo que LFPDPPP Art. 9 pide para datos sensibles. Debería vivir en onboarding.ts (nuevo campo `health_consent: z.literal(true)` en SubmitSchema) + un checkbox dedicado en el flujo de onboarding del frontend antes de enviar las respuestas de salud. |
| L3 | ⚠️ | 🟡 | La cancelación (DELETE) es admin-only (`req.user!.role !== 'admin'` → 403 en users.ts:1203-1204); no hay autoservicio ni flujo/bitácora de solicitudes ARCO (no existe tabla data_requests/arco_requests, ni botón 'eliminar mi cuenta' o 'solicitar mis datos' en frontend/src/pages/client). Hoy depende de que la clienta escriba por el contacto de Privacy.tsx y que el staff ejecute el cambio a mano sin registro auditable de la solicitud. El fix viviría en una tabla de solicitudes ARCO + endpoint en routes/users.ts + botón en el perfil del cliente. |
| L4 | ✅ | 🟠 | backend/src/index.ts:85-96 define apiLimiter global (600 req/min por IP, standardHeaders, excluye /api/health y webhooks) montado en index.ts:154; index.ts:97-108 define authLimiter (100/15min, solo POST) montado específicamente en `/api/auth` (index.ts:3669: `app.use('/api/auth', authLimiter, authRoutes)`), cubriendo login/register/forgot/reset. `app.set('trust proxy', 1)` (index.ts:77) asegura que el limiter cuente por IP real detrás del proxy de Railway. Checkout (orders/payments) queda cubierto por el limiter global. |
| L5 | ⚠️ | 🟠 | 25 de 48 archivos de rutas no importan zod: cash-shifts.ts, products.ts, sales.ts, loyalty.ts, facilities.ts, referrals.ts, reformers.ts, coach-payroll.ts, commissions.ts, reception-dashboard.ts, entre otros — usan checks manuales ad hoc e inconsistentes en su lugar (todos protegidos por authenticate+requireRole/requirePermission, así que el riesgo es de una sesión de staff comprometida o un bug de cliente, no del público). Falta agregar schemas Zod (o middleware de validación compartido) a esos archivos, priorizando los que mueven dinero: cash-shifts.ts, sales.ts, products.ts (stock-adjust), coach-payroll.ts, commissions.ts. |
| L6 | ⚠️ | 🔴 | lib/mercadopago.ts:90 tiene `if (!secret) return true;` — el path de MercadoPago falla ABIERTO (no cerrado) si MP_WEBHOOK_SECRET llegara a faltar (a diferencia del patrón correcto usado en stripe-webhook.ts). Aunque hoy el secreto SÍ está configurado en prod, es un defecto de diseño latente: una rotación de secreto mal hecha, un nuevo ambiente (staging) sin la variable, o un redeploy que la pierda, desactivaría la verificación de firma en silencio en vez de rechazar. Fix: en lib/mercadopago.ts:90, cambiar a fail-closed (rechazar si no hay secret configurado), igual que stripe-webhook.ts:13-14. |
| L7 | ❓ | 🟠 | No se puede confirmar ni negar desde el código. Falta verificar manualmente en el dashboard de Railway (servicio Postgres → pestaña Backups) que los respaldos automáticos estén activos, y documentar/probar un plan mínimo de recuperación (restaurar un snapshot en un ambiente de prueba). Si no están activos, activarlos o agregar un cron de `pg_dump` propio con retención. |
| L8 | ✅ | 🔴 | Se confirmó vía `railway variables -s backend` (nombres de variable, no valores citados aquí) que TODAS las credenciales de terceros —MP_ACCESS_TOKEN, MP_WEBHOOK_SECRET, APPLE_APNS_KEY_BASE64, APPLE_PASS_KEY_BASE64, APPLE_PASS_CERT_BASE64, APPLE_WWDR_BASE64, GOOGLE_SA_PRIVATE_KEY, GOOGLE_SERVICE_ACCOUNT_JSON, GOOGLE_REFRESH_TOKEN, JWT_SECRET, CHECKIN_SECRET, VAPID_PRIVATE_KEY— viven exclusivamente como variables de entorno de Railway. El código solo las lee vía `process.env.*` (lib/mercadopago.ts:13, lib/googleDrive.ts:23, routes/videos.ts:142, middleware/auth.ts:28, etc.); no existe ninguna migración/tabla que persista alguna de ellas en Postgres en texto plano (grep de mp_access_token/oauth_token/refresh_token como columna sin resultados). Esto corresponde a la opción 'env seguros' que la propia matriz acepta como cubierta. |

## EC

| ID | Estado | Sev. | Evidencia / Gap |
|---|---|---|---|
| EC1 | ✅ | 🔴 | backend/src/routes/bookings.ts:638-661 bloquea `SELECT ... FROM classes WHERE id=$1 FOR UPDATE` y revalida cupo DENTRO de la tx antes de insertar; lib/membershipSelection.ts:126-157 selectMembershipForBooking también usa FOR UPDATE sobre memberships; cancel_booking() (index.ts ~1893) bloquea la clase en el mismo orden clase→membresía. admin-book (bookings.ts:970-984) y bulk-month (bookings.ts:218-238, FOR UPDATE OF c) replican el mismo patrón. |
| EC2 | ✅ | 🔴 | Residual menor fuera del alcance de créditos: lib/loyalty.ts:333-339 awardCheckinPoints es un SELECT-then-INSERT sin lock ni constraint UNIQUE en loyalty_points(user_id, related_booking_id, type) — dos check-ins casi simultáneos del mismo booking podrían otorgar puntos de lealtad duplicados (no afecta créditos ni cupo). |
| EC3 | ❌ | 🟡 | Dos compras concurrentes con el mismo código de un solo uso restante pueden ambas pasar la validación (leen current_uses < max_uses) y ambas incrementar, sobrevendiendo el límite del código. Falta bloquear la fila (FOR UPDATE) o un UPDATE atómico condicional (`... WHERE current_uses < max_uses RETURNING`) en el momento de aplicar el descuento. |
| EC4 | ✅ | 🟠 | lib/membershipSelection.ts:126-143 selectMembershipForBooking filtra por `start_date <= classDate AND end_date >= classDate` (vigencia contra la fecha de LA CLASE, no 'hoy'); bookings.ts:709-720 aplica la misma regla en el path de membershipId explícito; bulk-month (bookings.ts:330-349) filtra las clases posteriores al end_date de la membresía elegida. Una sola política, aplicada igual en los 3 flujos. |
| EC5 | ❌ | 🟡 | La función de congelar membresía es inalcanzable desde la UI de admin/recepción (backend-only). Y aunque se invocara vía API directa, el pause no cancela ni marca las reservas futuras ya hechas contra esa membresía — quedan 'confirmed' silenciosamente, sin aviso ni decisión explícita. |
| EC6 | ✅ | 🟡 | lib/schedule.ts:8-21 cdmxWallClockToUtc usa Intl.DateTimeFormat dinámico (no offset fijo) para convertir hora de pared CDMX a UTC real; se usa consistentemente en bookings.ts:489 (crear reserva), checkin.ts:381,570,708 (gates de check-in QR/self/manual), y cancel_booking() (index.ts ~1898, `AT TIME ZONE 'America/Mexico_City'` en SQL). classes.date/start_time se guardan como DATE/TIME de pared, con un único helper como fuente de verdad. |
| EC7 | ❌ | 🟡 | No existe ningún guard tipo 'nunca entre 21:00 y 8:00' en el pipeline de recordatorios (in-app + push); un recordatorio matemáticamente correcto puede despertar a la clienta de madrugada. |
| EC8 | ⚠️ | 🟠 | El daño principal del EC (perder días de vigencia) está resuelto, pero el registro contable del pago (payments.created_at) no refleja la fecha real de la transferencia — solo la vigencia de la membresía la refleja. |
| EC9 | ✅ | 🔴 | routes/mercadopago-webhook.ts:41-51 `INSERT INTO payment_webhook_events ... ON CONFLICT (provider, event_key) DO NOTHING` antes de ejecutar cualquier efecto, respaldado por UNIQUE(provider, event_key) real (index.ts:3088). routes/stripe-webhook.ts:20-23 análogo con stripe_webhook_events.event_id como PRIMARY KEY (index.ts:989-990). finalizePaidOrder (lib/orderFulfillment.ts:27-41) además re-verifica bajo FOR UPDATE como defensa adicional. |
| EC10 | ⚠️ | 🔴 | Si el webhook se pierde TOTALMENTE (nunca llega ni una vez), mp_payment_id nunca se setea y el único endpoint de recuperación responde 400 'no hay pago que reconciliar' — y de todos modos es inalcanzable desde la UI. La clienta que pagó puede quedar sin membresía sin que nadie en el estudio tenga cómo repararlo desde el panel. |
| EC11 | ⚠️ | 🟠 | No hay cálculo proporcional/prorrateado expuesto por el sistema (todo o nada, al monto completo del pago) — el ajuste por clases ya tomadas queda en manos del criterio manual del staff, fuera del sistema. |
| EC12 | ⚠️ | 🟡 | No hay alerta a la dueña más allá de un console.warn en la apertura del dispute (stripe-webhook.ts:80) — ningún WhatsApp/email/push al admin en un contracargo confirmado. Tampoco se marca a la clienta en su ficha (no se inserta en suspicious_activity ni existe columna de flag) para que el staff la identifique en el futuro. |
| EC13 | ✅ | 🟠 | lib/membershipSelection.ts:75-97 pickBestMembership es la única función de ranking (acotada antes que ilimitada, vigencia más próxima, luego más antigua) y la usan: selectMembershipForBooking (bookings.ts:747-754, self-booking), admin-book (bookings.ts:987-994), y bulk-month (bookings.ts:290-322, reutiliza pickBestMembership directamente). |
| EC14 | ⚠️ | 🟡 | No existe ninguna métrica de conversión registrada (grep en routes/reports.ts y routes/stats.ts por is_free_booking/conversion/trial no encontró nada) — la parte de 'la conversión queda registrada (K3)' no está implementada. |
| EC15 | ⚠️ | 🟡 | Contradice 'anonimizar en vez de borrar lo contable': pagos/membresías se eliminan de verdad. No hay advertencia específica del saldo de créditos que se pierde ni un paso explícito de decisión de reembolso antes de borrar. |
| EC16 | ❌ | 🟡 | Si se cae el internet en la puerta en hora pico, el check-in (QR o manual) depende 100% de la red — no hay fallback ni forma de capturar asistencia offline para reconciliar después. |
| EC17 | ⚠️ | 🟠 | Todas esas llamadas son fire-and-forget con `.catch(e => console.error(...))` — si la actualización del pase falla (red, API caída), no hay reintento ni cola; el fallo se pierde en logs del servidor y el pase puede quedar desactualizado sin que nadie se entere. |
| EC18 | ✅ | 🟡 | No hay QR rotativo por-escaneo (la mejora opcional 'para estudios estrictos' que el propio EC marca como no obligatoria). |
| EC19 | ➖ | 🟠 | No aplica: el perfil de operación confirma que no se usan agregadores; el mecanismo de 'cupo de canal' que describe el EC no existe en este sistema. |
| EC20 | ➖ | 🔴 | No aplica RLS multi-tenant clásico. El scoping de sucursal que sí existe está bien implementado (fuente de verdad = sesión, no el body) en los endpoints revisados. |
| EC21 | ⚠️ | 🟡 | No existe ningún flag de 'entrega fallida' visible en el perfil ni en la UI de recepción — los fallos de WhatsApp desaparecen en logs del servidor. La parte 'H10 marca los fallos... y la recepción lo ve' no está implementada. |
| EC22 | ❌ | 🟡 | Una instructora que también quiere tomar/pagar clases como clienta necesita una SEGUNDA cuenta con otro email — identidad duplicada (saldo de lealtad separado, hilo de WhatsApp separado, aparece como dos personas distintas en admin), justo lo que el EC pide evitar. |
| EC23 | ✅ | 🟠 | routes/orders.ts: flujo completo de comprobante — la clienta sube proof (~796-887), la orden pasa a 'pending_verification', staff aprueba/rechaza con admin_notes (923-1008, 1158-1198), todo registrado vía logAction (audit trail). Adicionalmente /memberships/assign-cash permite a recepción registrar un pago directo. |
| EC24 | ✅ | 🟡 | Es un flujo compuesto de 2 pasos (cancelar + admin-book) hecho por staff, no un botón único de 'mover reserva' en la UI. |
| EC25 | ⚠️ | 🟡 | La oferta de congelación (D7, mismo endpoint /pause de EC5) es inalcanzable desde cualquier UI — los dos flujos (actualizar ficha de salud y ofrecer congelación) no están realmente conectados en la práctica, ya que congelar no se puede accionar desde el panel. |
